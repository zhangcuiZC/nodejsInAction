#pager
	if page.count > 1
		if page.number
			a(id="prev", href="/#{page.number}") Prev
		if page.number < page.count - 1
			if page.number
				i &nbsp;&nbsp;
			a(id="next", href="/#{page.number + 2}") Next
